# SPDX-License-Identifier: CC0-1.0

pisp_sources += files([
    'variant.cpp',
])

variants_headers = files([
    'variant.hpp'
])

variants_include_dir = join_paths(pisp_include_dir, 'variants')
install_headers(variants_headers, subdir: variants_include_dir)
